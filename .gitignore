# Nested auto-ping reference must never ship from this product tree
cpa-plugin-codex-auto-ping/
*.so
*.dylib
*.dll
*.h
*.sum
.worktrees/
dist/
package/
*.test

# Runtime last-run state (also created by tests using default path)
**/data/codex-selective-ping/
